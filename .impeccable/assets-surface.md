# Assets surface record

Scope: the reusable game asset catalog, detail/3D inspection, metadata editing and import flow inside Vault's existing Library. Visitor mode: working and finding, for a creator who wants to reuse a coherent asset without hunting for its textures, rig or animation files.

The task sequence is find a package, inspect what it contains, then get its complete files. Real supplied previews provide recognition; connected files, measured model statistics and provenance provide evidence. The catalog groups by reusable asset rather than presenting every dependency as an independent result. Style remains descriptive, independent of polygon count. Assets joins the existing sidebar above Entertainment and inherits the incumbent design world.

The implemented composition uses a dedicated submitted search (including tags), View and Add assets above compact filters. Small/medium/large grids and list view remain local preferences; changing views or selecting packages updates existing cards without a catalog request or preview reload. Type and Relationship distinguish standalone packages from connected model content, with Belongs to model keeping textures and animations attached to their model cards. VFX and Source files have explicit types. On phones, small/medium/large show three/two/one columns, while Filters and More filters & sorting collapse. The object preview remains the memorable element; related-file organization is the operational priority. Missing or unsupported previews retain an honest text state and download path.

The wrapping selection row exposes metadata editing, Get selected and reversible Trash/Restore. The inline bulk form preserves blank fields, explains consequences and offers explicit Apply/Cancel. Detail pairs inspection with metadata. Its stage and controls form one dark local panel under either Library theme; the component colors do not change Vault's global palette. Expand spans the content width, and browser fullscreen keeps wrapped controls inside the viewport. Textured/solid/wireframe surfaces and bones aid inspection. Clip seeking, frame steps, speed, loop and In/Out ranges remain temporary previews, reinforced by visible copy; they do not rewrite animation files.

Prepared source add-ons automatically join exactly one active package with the same source ID and collection, preserving the existing card identity, primary model, preview and metadata. The optional Amber Road Blender add-on contains 35 editable GLB conversions with packed textures and 12 confirmed rigs/animation sets. It is not recovered original sculpt history. Operational details remain in ASSETS.md.

Built sources: `public/assets.js`, `public/assets.css`, `public/asset-viewer.js`, and the Library navigation in `public/index.html`. Durable product behavior is in `PRODUCT.md`; visual scope is in the Assets sections of `DESIGN.md`; package setup and limitations are in `ASSETS.md`.

Evidence supplied for this documentation pass: `.impeccable/review/assets-final/desktop.png`, `detail.png`, `gold-3d.png`, `character-3d.png`, `import.png`; `.impeccable/review/assets-corrections/mobile.png`, `mobile-detail.png`, `light-actual.png`. These are browser captures with verification content, not bundled catalog assets. Documentation records the implementation and supplied evidence; it does not claim a fresh interaction test, a physical-device test or production-scale benchmark.

Workbench follow-up evidence: `.impeccable/review/assets-workbench-final/desktop.png`, `mobile.png`, `viewer.png`, `mobile-viewer.png`, `mobile-controls.png`, `expanded.png`, `fullscreen.png`, `light.png`, and `user-900.png`. The fresh reviewer returned **SHIP**, with no material findings. ASSETS-VERIFICATION.md records the browser, server and Blender checks, including the real source add-on import to an existing card. This merge is documentation only and does not add an independent QA claim.

Known boundaries: GLB-only 3D interpretation with self-contained resources and a 256 MB preview limit; other formats download. Saved assets persist privately on the Vault account across browsers and devices; legacy browser-local saves migrate on the next Assets visit from that browser. Versions are descriptive and declared checksums are not universally validated. Catalog scans have explicit bounds. Import recovery requires selecting the same unchanged files and keeping the transfer tab open.

The asset-actions refinement adds card right-click and a persistent three-dot keyboard/touch menu for Open, Edit details, Favorite, Get and Delete. Detail also has Delete. Its confirmation names the package, connected-file count and size before permanent removal of the full package; bulk Trash remains recoverable. Saving changes the private account preference in place without remounting detail previews; the Saved-only gallery refreshes its filtered results.

Image inspection retains the dark local panel and adds an image selector, height slider, desktop resize handle, Expand/Collapse and Fullscreen. Image preview returns from 3D within the asset. A question-circle beside Add assets in both gallery and form reveals an inline Library-themed guide: prepare/select/import, a collection folder tree beside one grouped card, related-file roles, tags, source-ID matching and an editable example asset.json download. The example stacks on phones; paths and recipes use monospace while prose retains the incumbent sans hierarchy.

Asset-actions evidence: `.impeccable/review/assets-actions/desktop.png`, `mobile.png`, `menu.png`, `detail.png`, `detail-mobile.png`, `delete-mobile.png`, `guide-desktop.png`, `guide-mobile.png`, `guide-light.png`, `image-expanded.png`, and `image-fullscreen.png`; interaction log `.qa/assets-actions-review.log`. The fresh reviewer returned **SHIP** after correction of stale Saved documentation. This targeted merge records supplied evidence and final source behavior, preserving the existing world without another visual or physical-device review.

Inspection/import follow-up: compact bottom icon tools preserve the approved dark viewing surface; Exit 3D returns to images even during loading or fullscreen. Mesh hides motion tools; Animate reveals them for animated files. Repeat prepared and ordinary folder imports automatically add missing paths to stable matching packages and preserve existing metadata. Conflicts and ambiguous matches stop without overwriting. The complete Amber Road export adds derived untextured GLBs and editable Blender conversions, preserving original media and recording honest provenance.
